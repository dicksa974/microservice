server.port=8762
spring.application.name=zuul-server
eureka.client.service-url.default-zone=http://localhost:8761/eureka/

# A prefix that can added to beginning of all requests.
#zuul.prefix=/api

# Disable accessing services using service name (i.e. gallery-service).
# They should be only accessed through the path defined below.
zuul.ignored-services=*

# Map paths to services
zuul.routes.gallery-service.path=/gallery/**
zuul.routes.gallery-service.serviceId=GALLERY-SERVICE

# Map paths to services
zuul.routes.users-service.path=/users/**
zuul.routes.users-service.serviceId=USERS-SERVICE

# Map paths to services
zuul.routes.image-service.path=/image/**
zuul.routes.image-service.serviceId=IMAGE-SERVICE

# Map paths to services
zuul.routes.pets-service.path=/pets/**
zuul.routes.pets-service.serviceId=PETS-SERVICE

# Map path to auth service
zuul.routes.auth-service.path=/auth/**
zuul.routes.auth-service.serviceId=AUTH-SERVICE

# By default, all requests to gallery service for example will start with: "/gallery/"
# What will be sent to the gallery service is what comes after the path defined,
# So, if request is "/gallery/view/1", gallery service will get "/view/1".
# In case of auth, we need to pass the "/auth/" in the path to auth service. So, set strip-prefix to false
zuul.routes.auth-service.stripPrefix=false

# Exclude authorization from sensitive headers
zuul.routes.auth-service.sensitiveHeaders=Cookie,Set-Cookie